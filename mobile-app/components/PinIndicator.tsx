import React from 'react';\nimport { View, Text, TouchableOpacity } from 'react-native';\nimport { Pin } from 'lucide-react-native';\nimport { usePinStore } from '@/hooks/use-pin-store';\n\ninterface PinIndicatorProps {\n  chatId: string;\n  onPress?: () => void;\n  size?: 'sm' | 'md' | 'lg';\n}\n\nexport const PinIndicator: React.FC<PinIndicatorProps> = ({ \n  chatId, \n  onPress, \n  size = 'md' \n}) => {\n  const pinnedMessages = usePinStore(state => state.getPinnedMessages(chatId));\n  const count = pinnedMessages.length;\n\n  if (count === 0) return null;\n\n  const getSize = () => {\n    switch (size) {\n      case 'sm': return { icon: 12, text: 10, padding: 4 };\n      case 'md': return { icon: 14, text: 12, padding: 6 };\n      case 'lg': return { icon: 16, text: 14, padding: 8 };\n      default: return { icon: 14, text: 12, padding: 6 };\n    }\n  };\n\n  const sizes = getSize();\n\n  return (\n    <TouchableOpacity\n      onPress={onPress}\n      className=\"flex-row items-center rounded-md border\"\n      style={{\n        backgroundColor: 'rgba(99, 102, 241, 0.1)',\n        borderColor: 'rgba(99, 102, 241, 0.2)',\n        paddingHorizontal: sizes.padding,\n        paddingVertical: sizes.padding / 2,\n      }}\n    >\n      <Pin \n        size={sizes.icon} \n        color=\"#6366F1\" \n        style={{ transform: [{ rotate: '45deg' }] }}\n      />\n      <Text \n        style={{ \n          color: '#6366F1', \n          fontSize: sizes.text, \n          fontWeight: '600',\n          marginLeft: 4\n        }}\n      >\n        {count}\n      </Text>\n    </TouchableOpacity>\n  );\n};\n\nexport const PinBadge: React.FC<{ count: number }> = ({ count }) => {\n  if (count === 0) return null;\n\n  return (\n    <View \n      className=\"flex-row items-center rounded-full\"\n      style={{\n        backgroundColor: '#6366F1',\n        paddingHorizontal: 6,\n        paddingVertical: 2,\n      }}\n    >\n      <Pin \n        size={10} \n        color=\"white\" \n        style={{ transform: [{ rotate: '45deg' }] }}\n      />\n      <Text \n        style={{ \n          color: 'white', \n          fontSize: 10, \n          fontWeight: 'bold',\n          marginLeft: 2\n        }}\n      >\n        {count}\n      </Text>\n    </View>\n  );\n};